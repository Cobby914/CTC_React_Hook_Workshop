{
  "name": "ctc-react-hook-workshop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd client && yarn dev\" \"cd server && yarn start\"",
    "dev": "concurrently \"cd client && yarn dev\" \"cd server && yarn dev\"",
    "install:all": "concurrently \"cd client && yarn install\" \"cd server && yarn install\"",
    "postinstall": "cd client && yarn install && cd ../server && yarn install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
